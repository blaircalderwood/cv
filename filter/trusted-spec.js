describe('trusted', function() {

    beforeEach(module('cv'));

    it('should ...', inject(function($filter) {

        var filter = $filter('trusted');

        expect(filter('input')).toEqual('output');

    }));

});
